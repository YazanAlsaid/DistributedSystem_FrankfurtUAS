/*package Java.DistributedSystem.sheet1;

import java.util.Scanner;

import static resources.Utility.checkInput;

public class runMultipleThread extends myThread{

    public runMultipleThread(int num) throws InterruptedException {
        super(num);
    }
    public runMultipleThread(){}
    public void runMyThreads() throws InterruptedException {

        Scanner scann = new Scanner(System.in);

        while(checkInput(scann.nextLine())){
            myThread thread = new myThread();
        }

    }
}
*/